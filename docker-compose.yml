version: '3.9'

services:
  mongodb:
    image: mongo:latest
    container_name: vehicle-record-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
    name: vehicle-record-mongo-data
